# 生产状态与验收记录

记录日期：2026-08-31（Asia/Shanghai）

## 线上状态

- Cloudflare Worker 已部署，`/healthz` 正常。
- GitHub OAuth 已完成，只有白名单账号 `liangke91-art` 可授权。
- ChatGPT App `Universal CalDAV Calendar` 已创建、发现全部工具并连接成功。
- fruux CalDAV 已通过一次性 HTTPS 设置页配置；真实凭据未写入仓库、项目文件或聊天。
- 默认时区为 `Asia/Shanghai`，默认提醒为 30 分钟，可在安全设置页修改。

## 生产端到端验收

以下项目均通过真实链路 `ChatGPT → 远程 MCP → fruux` 验证，测试日程随后全部删除：

- 账户状态与两个日历发现；
- 普通日程创建、范围查询、文本搜索、标题与时间修改、删除；
- 描述、地点与 10 分钟提醒往返读取；
- 冲突检查返回 1 个重叠日程；
- 空闲时间查询返回 8 个候选段，均避开忙碌区间；
- 每日重复、共 2 次的系列正确展开为 2 个实例；
- 第二个重复实例可被冲突检查识别；
- 使用主 UID 删除整个重复系列，删除后查询为 0。

## 已修复的兼容问题

fruux 的 CalDAV XML 响应会把 CRLF 中的回车保留为数字 XML 实体 `&#13;`。服务现会在 RFC 5545 行展开前恢复该传输换行，并包含对应回归测试。修复后的生产查询和重复实例展开均已通过。

## 剩余人工验收

只剩华为手机实际录入 `Huawei HarmonyOS` 设备凭据并观察一次双向同步。该步骤必须在手机与 fruux 安全页面中完成，见 [HARMONYOS.md](./HARMONYOS.md)。
