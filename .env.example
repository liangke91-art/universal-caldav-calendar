# Reference only. For local Wrangler development, copy these names to .dev.vars.
# For production, enter each value with `wrangler secret put`; never commit real values.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
ALLOWED_GITHUB_LOGINS=your-github-login
CREDENTIALS_MASTER_KEY=
